{
  "name": "dsh-desktop",
  "version": "0.1.4",
  "private": true,
  "description": "Desktop client for the DeepSeek Harness browser UI (dsh web)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "bundle:runtime": "node scripts/bundle-runtime.mjs",
    "version": "node scripts/sync-version.mjs && git add package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
